var read= require('readline-sync');
var util=require('../FunctionalUtility/FUtility');

    var string=read.question("Enter the String : ");
   util.permutation1(string,first);
  //  console.log(str);
